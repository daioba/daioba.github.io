# TokenDrift Project Page

Put project-page images in `tokendrift/static/images/`.

Use relative paths from `tokendrift/index.html`, for example:

```html
<img src="static/images/tokendrift_method.png" alt="TokenDrift method" loading="lazy">
```
